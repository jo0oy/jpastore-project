spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 500
    database-platform: org.hibernate.dialect.H2Dialect

logging:
  level:
    org:
      hibernate.SQL: debug
      hibernate.type: trace

      springframework:
        transaction: debug
        orm.jpa: debug
